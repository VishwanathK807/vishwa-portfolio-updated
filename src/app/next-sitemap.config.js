// next-sitemap.config.js
/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://vishwa-portfolio-updated.vercel.app',
  generateRobotsTxt: true, // optional
  changefreq: 'weekly',
  priority: 0.7,
};

export default config;
